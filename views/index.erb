<h2>Car List</h2>
<ul class='list-group'>
    <% @cars.each do |car|%>
        <li class='list-group-item'>
            <a href='/car/<%= car.id %>'><%= "#{car.id.to_s + ') ' + car.model + ' ' + car.year_birth.to_s}" %></a>
        </li>
    <% end %>
</ul>